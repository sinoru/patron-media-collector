{
  "name": "patron-media-downloader",
  "private": true,
  "version": "0.1",
  "description": "A WebExtension for downloading media from pixiv FANBOX, Patreon, etc.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "ext-build": "npm run build && web-ext build"
  },
  "devDependencies": {
    "vite": "^5.0.8",
    "web-ext": "^7.11.0"
  },
  "webExt": {
    "sourceDir": "dist/",
    "artifactsDir": "dist/web-ext-artifacts"
  }
}
